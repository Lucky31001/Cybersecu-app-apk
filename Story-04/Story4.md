Décompiler un APK
----------
Pour décompiler un apk nous avons utiliser la commande suivante de l'outil apktool:

```apktool d -a .\app.apk```

Cela nous a donné le dossier disponible à côté de ce Markdown 